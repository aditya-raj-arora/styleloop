fastapi
uvicorn[standard]
sqlalchemy>=2
alembic
pydantic-settings
email-validator
psycopg2-binary
python-multipart
redis
rq
passlib[argon2]
pyjwt
boto3
pillow
numpy
httpx
sentry-sdk
# rembg is intentionally NOT pinned here — it's a heavy, optional local
# bg-removal backend (onnxruntime + a ~176MB model download) imported lazily
# in services/bg_removal.py, which falls back to the remove.bg HTTP API when
# it isn't installed. Install it explicitly in the worker's environment:
#   pip install rembg
