// Dashboard page — stub.
// Wireframe: today's weather-aware outfit from GET /outfits/daily + regenerate.
// TODO(Frontend): render daily outfit, weather chip, regenerate button.
export default function Dashboard() {
  return (
    <main>
      <h1>Dashboard</h1>
      <p>TODO: today&apos;s suggestion from GET /outfits/daily.</p>
    </main>
  );
}
