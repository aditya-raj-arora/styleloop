// Login page — stub.
// Wireframe: email + password form -> POST /auth/login -> store JWT via useAuth.
// TODO(Frontend): build the form and wire the login mutation.
export default function Login() {
  return (
    <main>
      <h1>Login</h1>
      <p>TODO: email + password form → POST /auth/login → useAuth.setToken(jwt).</p>
    </main>
  );
}
