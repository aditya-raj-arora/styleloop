// Swipe page — stub.
// Wireframe: swipe like/dislike on outfit suggestions -> POST /outfits/:id/feedback.
// TODO(Frontend): swipe deck UI wired to the feedback endpoint.
export default function Swipe() {
  return (
    <main>
      <h1>Swipe</h1>
      <p>TODO: swipe deck → POST /outfits/:id/feedback.</p>
    </main>
  );
}
