// Wardrobe page — stub.
// Wireframe: grid of GarmentCard from GET /garments (uses the Garment contract).
// TODO(Frontend): useQuery(['garments']) → render GarmentCard grid, filter by state.
export default function Wardrobe() {
  return (
    <main>
      <h1>Wardrobe</h1>
      <p>TODO: grid of GarmentCard from GET /garments.</p>
    </main>
  );
}
